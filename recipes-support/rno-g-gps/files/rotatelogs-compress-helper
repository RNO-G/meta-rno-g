#! /bin/sh

# this is a program meant to be invoked from rotatelogs. rotatelogs will give the rotated-from file (if it exists) as the second argument

compressme="${2}"
compresscmd="zstd --rm"


if [[ "${compressme}" ]]; then
  ${compresscmd} ${compressme}
fi

